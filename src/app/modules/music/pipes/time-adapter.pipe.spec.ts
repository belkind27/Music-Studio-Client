import { TimeAdapterPipe } from './time-adapter.pipe';

describe('TimeAdapterPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeAdapterPipe();
    expect(pipe).toBeTruthy();
  });
});
